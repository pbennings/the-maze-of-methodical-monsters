const player = {
  isAlive: true,
  hitPoints: 100,
  level: 1,
  lastDamageTaken: 0,
  flight: function(monster) {
    this.lastDamageTaken =
  }

  levelUp: function() {
  this.levelUp = true;
  this.hitPoints = 100;
  },
}